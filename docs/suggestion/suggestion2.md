# Align project prepare-for-docker skill with three-script + cloud-admin contract

**Status:** Done (skill updated to match `create-image.ps1` / local / server scripts, `/cloud-admin` paths, and `VOLUME_DIR` → `/data`).

**Why it mattered:** Agents following the old project skill recreated a single `run-on-docker.ps1` and `/opt/docker` paths.

**Effort:** Small — keep skill in sync when deploy paths or script names change again.
